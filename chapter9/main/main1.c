#include<stdio.h>
int main(void)
{
	/*
	1.形参是定义在被调函数中的变量，实参是出现在函数调用中的值，该值被赋给形参
	
	2.
	  a. void dount(int);
	  b. int gear(int,int);
	  c. int guess(void);
	  d. stuff_it(double,double *);
	3.
	  a. char n_to_char(int);
	  b. int digit(double,int);
	  c. double* which(double*,double*);
	  d. int random(void);
	  */

	return 0;
}